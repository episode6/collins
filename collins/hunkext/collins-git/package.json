{
  "name": "collins-git",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "description": "Collins' hunk extension: hunk, file and line staging, a line-range anchor and discard at hunk's cursor",
  "hunk": {
    "apiVersion": 8,
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "hunkdiff": "^0.21.1",
    "typescript": "^5.9"
  }
}
