<?xml version="1.0" encoding="UTF-8"?>
<svg width="22" height="22" viewBox="0 0 22 22" xmlns="http://www.w3.org/2000/svg">
  <!--
  Empty variant of com.episode6.Collins-panel.svg (original artwork for the
  ghackett fork): the same 22-grid glass and straw with nothing poured in.
  The status icon exports it when nothing is running and nothing is unread —
  no pole to show and no badge to wear — so a glance at the panel says
  "nothing waiting" without a number. The drink comes back with the first
  unread notification, the pole with the first session to start working.

  Keep this comment INSIDE the root element: gdk-pixbuf only recognizes an
  SVG when the <svg> tag appears within its sniff buffer, and a long pre-root
  comment pushes it out, leaving the icon blank.

  The ice cube goes with the drink: a lone cube in a dry glass reads as a
  smudge at 22 pixels, and the glass's own outline is the whole message.
  Same keylines as the full glass, and for the same reason — with no tile
  and now no pour, the cream outline has nothing but the panel behind it.
  -->

  <!-- keylines: the same shapes, a shade wider, under everything -->
  <g stroke="#262320" fill="none" opacity="0.5" stroke-linecap="round">
    <line x1="14.3" y1="2.2" x2="11.6" y2="8" stroke-width="2.6"/>
    <rect x="6" y="3" width="10" height="16" rx="2" stroke-width="3"/>
  </g>

  <!-- straw: clears the rim by a hair, no more — at 22px a longer overhang
       is a horn on the corner rather than a straw -->
  <line x1="14.3" y1="2.2" x2="11.6" y2="8" stroke="#EDE9E1" stroke-width="1.8"
        stroke-linecap="round"/>

  <!-- glass: 2 wide on whole-pixel edges, so 5..17 and 2..20 stay crisp -->
  <rect x="6" y="3" width="10" height="16" rx="2" fill="none" stroke="#EDE9E1"
        stroke-width="2"/>
</svg>
